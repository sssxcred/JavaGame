import controller.GameManager;

/**
 * The Main class contains the entry point for the game application.
 * It initializes the GameManager to start the game.
 */
public class Main {
    /**
     * The main method, entry point for the game application.
     *
     * @param args Command-line arguments (not used).
     */
    public static void main(String[] args) {
        new GameManager();
    }
}
